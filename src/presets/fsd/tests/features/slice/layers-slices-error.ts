import { appLib } from '@/app/slice';
import { pageModel } from '@/pages/slice';
import { widgetModel } from '@/widgets/slice';
import { featureModel } from '@/features/slice';

export const foo = { appLib, pageModel, widgetModel, featureModel };
