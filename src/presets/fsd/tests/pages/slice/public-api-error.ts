import { widgetModel } from '@/widgets/slice/model';
import { entityModel } from '@/entities/slice/model';
import { featureModel } from '@/features/slice/model';

export const foo = { widgetModel, entityModel, featureModel };
